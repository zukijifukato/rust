fn main() {
  // We can comment rust code in middle of the line
  /*
   * Block comments are cool to comment chunks of code
   * fn hello() {
   *   Hello;
   * }

    Star is not compulsory but style preference.
  */
  let x = 5 + /* 5 + */ 5;
  println!("{}", x);
}
